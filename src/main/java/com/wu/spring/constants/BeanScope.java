package com.wu.spring.constants;

/**
 * Bean作用域常量
 * @author Cactus
 */

public enum BeanScope {
    /**
     * SINGLETON 为单例模式，由于实现原因，暂时默认为单例模式。
     * PROTOTYPE 原型模式
     */
    SINGLETON,
    PROTOTYPE
}
